import { Canvas, Controls, Meta, Stories, Story } from '@storybook/addon-docs/blocks'

import meta, * as BNAppHeaderStories from './BNAppHeader.stories'

<Meta of={meta} />

# BNAppHeader
The `BNAppHeader` is a light wrapper around Material UI's `AppBar` component, with composible helper components 
for easy composibility of application headers.

<Canvas of={BNAppHeaderStories.Default} />
